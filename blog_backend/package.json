{
  "name": "blog_backend",
  "version": "1.0.0",
  "main": "index.js",
 "scripts": {
  "start": "npx prisma generate && npx prisma migrate deploy && node app.js",
  "dev": "npx prisma generate && npx prisma migrate deploy && nodemon app.js",
  "test": "echo \"Error: no test specified\" && exit 1"
},
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.9.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "nodemon": "^3.1.11",
    "prisma": "^5.22.0"
  }
}
